name: CD

on:
  push:
    branches: [ main ]
    tags: [ "v*" ]
    paths-ignore:
      - 'assets/**'
      - 'README.md'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # Tag-only verification
  verify-charts:
    if: github.ref_type == 'tag'
    uses: ./.github/workflows/verify-charts.yaml

  # Build wheels (runs for all events)
  build:
    uses: ./.github/workflows/build.yaml

  # Main branch only: benchmarks
  pyperformance:
    if: github.ref == 'refs/heads/main'
    needs: build
    uses: ./.github/workflows/pyperformance-benchmarks.yaml

  benchmarks-and-assets:
    if: github.ref == 'refs/heads/main'
    needs: build
    uses: ./.github/workflows/benchmarks-and-assets.yaml

  # Build sdist (main branch or tag)
  sdist:
    needs: [build, benchmarks-and-assets, verify-charts]
    if: (github.ref == 'refs/heads/main' || github.ref_type == 'tag') && !cancelled() && !failure()
    uses: ./.github/workflows/sdist.yaml

  # Publish to PyPI (main branch or tag)
  # Kept inline as reusing workflows is not supported for PyPI publishing
  publish:
    needs: sdist
    if: (github.ref == 'refs/heads/main' || github.ref_type == 'tag') && !cancelled() && !failure()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/project/copium
    steps:
      - name: Prepare directories
        run: |
          rm -rf artifacts dist
          mkdir -p artifacts dist

      - name: Download Wheels
        uses: actions/download-artifact@v4
        with:
          pattern: "Wheels-*"
          path: artifacts
          merge-multiple: true

      - name: Download sdist
        uses: actions/download-artifact@v4
        with:
          name: sdist
          path: artifacts

      - name: Flatten artifacts into ./dist
        shell: bash
        run: |
          shopt -s globstar nullglob
          # Copy wheels and sdists from any artifact subfolder into ./dist
          for f in artifacts/**/*.whl artifacts/**/*.tar.gz; do
            cp -v "$f" dist/
          done
          echo "Contents of dist:"
          ls -l dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          skip-existing: true
